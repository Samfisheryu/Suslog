package app.suslog.ui.theme

import androidx.compose.ui.graphics.Color

// Telemetry light palette — engineering paper + green-ink accent.
// Mirrors the approved Tuning design preview (charcoal-on-paper instrument look).

val Paper = Color(0xFFE9ECE5)       // app background
val PanelWhite = Color(0xFFFFFFFF)  // cards / surfaces
val PanelMuted = Color(0xFFF6F8F2)  // raised / secondary surface
val Inset = Color(0xFFE7EAE2)       // strips / inset chips
val LineSoft = Color(0xFFE0E4D9)    // hairlines / grid
val Line = Color(0xFFD2D7CB)        // borders

val LimeInk = Color(0xFF5C7D12)     // primary accent — text & strokes
val LimeBright = Color(0xFFBDEE36)  // highlight fills
val LimeDim = Color(0xFFA7BC6A)     // accent borders

val Cyan = Color(0xFF1593A6)        // secondary / predicted
val Amber = Color(0xFFBF7D14)       // tertiary / warning
val SignalRed = Color(0xFFCF3B3B)   // error / low confidence

val InkHigh = Color(0xFF171C1D)     // primary text
val InkMute = Color(0xFF55615E)     // secondary text
val InkFaint = Color(0xFF8A9590)    // tertiary text / labels
